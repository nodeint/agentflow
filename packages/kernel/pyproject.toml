[project]
name = "agentflow-kernel"
version = "0.1.0"
description = "Workflow kernel for the Agentflow runner."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agentflow_kernel"]
